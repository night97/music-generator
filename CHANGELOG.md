# 音乐生成器 - 版本迭代文档

## 版本管理工作流程

```
1. 提出需求/想法
       ↓
2. 在 CHANGELOG.md 规划待开发内容（添加到 [待开发] 或 [计划中]）
       ↓
3. 确认开发内容和方案
       ↓
4. 创建功能分支开发（git checkout -b feature/xxx）
       ↓
5. 开发完成后 PR/Merge 到 main
       ↓
6. 更新 CHANGELOG.md 标记为正式版本
```

---

## 版本历史

### v1.2.0 (2026-04-29)

#### 新增功能

- **抽卡批次后台串行生成**
  - 每批 10 条提示词，逐条串行生成
  - 支持批次终止、继续生成、失败项重试
  - 支持超时阈值配置（默认 8 分钟）
  - 批次记录分页展示（每批一页）
- **质量模式**
  - 新增独立菜单入口
  - 支持 AI 对话教练迭代“高质量目标”
  - 支持变体提示词生成、打分排序、选 Top-N 提交批次
  - 质量模式批次记录独立范围查看（`scope=quality`）
- **歌词质检与修复**
  - 新增 `宽松/标准/严格` 三档质检严格度
  - 规则覆盖押韵、副歌长度、句长变化、风格一致性
  - 新增“押韵修复器”：质检失败时优先副歌定向润色后复检
- **歌曲详情页增强**
  - 支持歌词时间轴手动编辑与保存
  - 支持默认间隔批量应用、区间应用、手动排序

#### 技术改进

- **提示词优化稳健性提升**
  - 兼容 `thinking + text` 混合返回
  - 支持 Markdown 包裹、非标准 JSON、截断文本兜底解析
  - 优化失败时回退原提示词继续执行，不中断整批
- **翻唱（Music Cover）流程改造**
  - `/api/generate` 改为两步流程：`music_cover_preprocess` → `music_generation`
  - 自动注入 `cover_feature_id` 与可用歌词字段
  - 为 cover 预处理增加超时与重试机制
- **日志能力增强**
  - 新增结构化 `[GENERATE][INFO/WARN/ERROR]` 日志
  - 细化任务阶段、重试次数、耗时、错误归因
  - 批次日志持续写入 `data/logs/gacha-batch.log`
- **音频落地策略优化**
  - 批量生成结果默认以 HEX 落本地文件，避免外链 24 小时失效
  - 歌词与任务结果同步持久化到本地数据文件

#### 修复

- 修复高质量模式批次记录跳转到抽卡记录范围的问题
- 修复“有结果但提示词优化解析失败”的误判问题
- 修复翻唱链路中 `audioUrl/audioBase64` 参数未正确参与实际生成流程的问题

### [待开发] - 开发中

#### 功能优化

- [x] **一键生成**：输入关键词自动生成完整描述+歌词，直接开始生成，无需手动填写表单
- [ ] **提示词模板库**：预设不同音乐风格的提示词模板
- [x] **随机抽卡模式**：AI 读取热门话题生成关键词，一键批量生成 10 首歌曲并统一展示
- [ ] **批量生成对比**：一次性生成多个版本便于对比选择
- [ ] **收藏/标记**：收藏喜欢的生成结果

#### 用户体验

- [x] **同步歌词显示**：播放音乐时显示歌词，无歌词则不显示面板
  - 静态模式：用户手动输入的歌词，按行均分总时长估算显示
  - 同步模式：LRC 格式带时间轴歌词，精确同步高亮
  - AI 生成歌词：自动关联到生成的音频
- [ ] **生成历史预览**：列表形式快速预览历史生成
- [ ] **提示词优化建议**：根据生成结果给出提示词调整建议

---

### [计划中] - 已规划

- [ ] **音乐风格分类标签**：给生成的音乐打上风格标签便于筛选
- [ ] **导出功能增强**：支持导出不同格式、批量导出

---

### v1.1.0 (2026-04-22)

#### 新增功能

- **同步歌词显示**：播放音乐时显示歌词，无歌词则不显示面板
  - 静态模式：用户手动输入的歌词，按行均分总时长估算显示
  - 同步模式：LRC 格式带时间轴歌词，精确同步高亮
  - 卡拉OK式高亮效果，自动滚动当前行
  - 深色主题 UI，与播放器风格统一

#### 技术改进

- 新增 `src/lib/lrc.ts`：LRC 歌词解析工具
- 新增 `src/components/LyricsDisplay.tsx`：歌词展示组件

---

### v1.0.0 (2026-04-22) - 初始版本

#### 新增功能

- 基于 MiniMax API 的音乐生成（Music 2.6 / Music Cover 模型）
- **AI 对话助手**：通过对话引导完善音乐描述提示词
- AI 歌词生成功能（支持创作新歌词和续写）
- 历史记录管理
- 流式响应聊天界面

#### 技术栈

- Next.js 14 + TypeScript
- Tailwind CSS
- MiniMax API（音乐生成 + 歌词生成 + 文本对话）

#### 文件结构

```
src/
├── app/
│   ├── api/
│   │   ├── chat/          # AI 对话 API
│   │   ├── generate/      # 音乐生成 API
│   │   ├── history/       # 历史记录 API
│   │   ├── lyrics/        # 歌词生成 API
│   │   └── task/[id]/     # 任务状态查询
│   ├── history/           # 历史记录页面
│   └── page.tsx          # 首页
├── components/
│   ├── AudioPlayer.tsx   # 音频播放器
│   ├── MusicChat.tsx     # AI 对话组件
│   └── MusicForm.tsx     # 音乐生成表单
└── lib/
    └── storage.ts        # 本地存储
```

---

## 如何添加新版本

当完成一个版本开发后，在对应版本号下添加：

```markdown
### v1.1.0 (2026-04-XX)

#### 新增功能
- 功能 A
- 功能 B

#### 优化
- 优化项 C

#### 修复
- 修复问题 D
```

---

## 版本号规范

- **主版本 (x.0.0)**：不兼容的 API 或重大架构调整
- **次版本 (1.x.0)**：新增功能，向后兼容
- **修订版 (1.0.x)**：向后兼容的问题修复或优化
